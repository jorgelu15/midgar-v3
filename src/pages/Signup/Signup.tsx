import { useTitle } from "../../hooks/useTitle";

const Signup = () => {
        useTitle();
    
    return ( 
        <div>
            <h1>Registrarse</h1>
        </div>
     );
}
 
export default Signup;